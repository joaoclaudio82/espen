{
  "name": "espen-sistema-gestao-competencias",
  "version": "1.0.0",
  "description": "ESPEN – Sistema de Gestão por Competências – SENAPPEN/DEPEN",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=5500 --open=index.html",
    "dev": "npx live-server --port=5500 --open=index.html --watch=.",
    "lint": "npx eslint js/**/*.js",
    "api:install": "pip install -r backend/requirements.txt",
    "api:dev": "uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8001"
  },
  "keywords": ["espen", "senappen", "competencias", "gestao", "pdi"],
  "author": "Reginaldo Manoel Teixeira",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "eslint": "^8.0.0"
  },
  "browserslist": ["> 1%", "last 2 versions", "not dead"]
}
